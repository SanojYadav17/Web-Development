const { add, name, age, role, obj } = require("./math");

console.log(add(2, 3));
console.log(`I am ${name} and my age is ${age} and I am a ${role}`);

console.log(obj.name);
console.log(obj.age);
console.log(obj.role);